"""
RCT - Representation Contract Tests

These tests validate that the system correctly round-trips its core data
across real boundaries (storage, CLI output parsing, etc.).

RCT tests must be green before proceeding with feature implementation.
"""
