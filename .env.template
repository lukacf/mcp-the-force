# Copy to .env and fill in locally
# DO NOT commit .env file

# Required for Google Cloud auth with user credentials
GCLOUD_USER_REFRESH_TOKEN=

# Optional - only if using custom OAuth app (defaults to gcloud CLI app)
GCLOUD_OAUTH_CLIENT_ID=
GCLOUD_OAUTH_CLIENT_SECRET=